# KGroups_Bio
This repository contains the code files of a filter feature selection algorithm I designed and developped during my PhD studies at University College Dublin (UCD) in Ireland.   My algorithm (KGroups) uses clustering for feature selection instead of sorting (in descending order) or an incremental/greedy algorithm such as sequential forward search.
